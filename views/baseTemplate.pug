//-This is the main page for the layout i.e. base layout. This layout consists the main header and
//- the footer that is common to all pages of the website

//- There are two types of comments // (appears in the rendered html file as a comment) and 
    //- (doesnt appear anywhere)


//- To include a partial file, we use include function with the full name of the file

doctype html
html
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0' script-src 'self' api.mapbox.com;)
            link(href='https://fonts.googleapis.com/css?family=Lato:300,300i,700'
            rel='stylesheet')
            link(rel='stylesheet' href='/css/style.css')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            title Natours | #{title}


    body(data-alert=`${alert?alert:''}`)
        //Header
        include _header

        //Content
        block content   
            h1 This is the placeholder text

        //Footer
        include _footer
        
        script( src='https://js.stripe.com/v3/')
        script(src= '/js/bundle.js' )


        


    